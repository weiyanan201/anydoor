安装supervisor 监控文件的变化，重启node服务器
启动命令改为 supervisor app.js


vscode 改变字体大小："editor.fontSize": 12

handlebars html模板引擎

压缩 gzip deflate


curl :
    -i 打印响应头和结果
    -r 0-10 返回10个字节的结果

http：
    200
    206：部分结果


缓存header：
    expires
    Cache-Control(相对时间)
    If-Modified-Since/Last-Modified
    If-None-Match/ETag

命令行工具包：
    yargs
    commander

npm 将包信息保存在package.json 要添加参数 --save   --save-dev




vscode使用技巧：
    选取多个变量同时修改，按住alt再选取


本地构建：gulp
